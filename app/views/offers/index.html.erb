<div class="container py-4">
    <% if @offers.any? %>
      <% @offers.each do |offer| %>
      <%= render "offer", offer: offer %>
      <% end %>
    <% else %>
      No offers for <%= @listing.pets.map(&:name).to_sentence %> yet — a neighbour will turn up 🐾
    <% end %>

</div>
